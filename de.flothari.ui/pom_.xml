
<!-- pom nur benutzen um Mavendependencies nach /lib zu kopiern, danch 
	 wieder umbenennen in pom_ sonst funktioniert der Buildprozess aus dem
	 im TopLevel-Verzeichnis nicht. 
      !!! run: 'mvn clean package' headless - nicht in der PDE-->
<project>
  <modelVersion>4.0.0</modelVersion>

  <groupId>de.flothari</groupId>
  <artifactId>de.flothari.ui</artifactId>
  <version>1.0.0-SNAPSHOT</version>
 
  <dependencies>
    <dependency>
      <groupId>org.opencv</groupId>
      <artifactId>opencv</artifactId>
      <version>4.6.0</version>
    </dependency>
  </dependencies>

  <build>
    <plugins>
     <!-- Nur zum Kopieren von 'opencv' jar  ins Plugin /lib Verzeichnis -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-dependency-plugin</artifactId>
        <version>3.6.1</version>
        <executions>
          <execution>
            <id>copy-opencv-jar</id>
            <phase>process-resources</phase>
            <goals><goal>copy-dependencies</goal></goals>
            <configuration>
              <outputDirectory>${project.basedir}/lib</outputDirectory>
              <includeArtifactIds>opencv</includeArtifactIds>             
              <stripVersion>true</stripVersion>
               <!-- 'false' wuerde die versionsnummer von 'opencv' beibehalten -->
            </configuration>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>
</project>
